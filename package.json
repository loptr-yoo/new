{
  "name": "building-generator-backend-only-root",
  "private": true,
  "scripts": {
    "dev:backend": "python -m uvicorn building.app.main:app --reload --host 127.0.0.1 --port 8000",
    "test:building": "python -m pytest building/tests -q"
  }
}
